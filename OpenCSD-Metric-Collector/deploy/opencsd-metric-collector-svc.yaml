apiVersion: v1
kind: Service
metadata:
  name: opencsd-metric-collector
  namespace: management-controller
  labels:
    name: opencsd-metric-collector
    namespace: management-controller
spec:
  type: NodePort
  ports:
    - port: 40801
      targetPort: 40801
      nodePort: 30801
      protocol: TCP
  selector:
    name: opencsd-metric-collector
    namespace: management-controller
